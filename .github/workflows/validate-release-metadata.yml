name: Validate Release Metadata

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - "release.json"
      - ".github/workflows/validate-release-metadata.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-release-metadata:
    name: validate-release-metadata
    uses: runlix/build-workflow/.github/workflows/validate-release-metadata.yml@23f7b95af8695f313051859a14a9815dbceb55fb
    with:
      release-json-path: release.json
      tool-image: ghcr.io/runlix/build-workflow-tools:sha-23f7b95af8695f313051859a14a9815dbceb55fb
