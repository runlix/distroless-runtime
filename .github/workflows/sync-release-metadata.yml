name: Sync Release Metadata

on:
  workflow_run:
    workflows:
      - Release
    branches:
      - release
    types:
      - completed

permissions:
  actions: read
  contents: write

jobs:
  sync:
    uses: runlix/build-workflow/.github/workflows/sync-release-metadata.yml@86e3cbfc7b3a8093801f36bbfb2d7245bb50cb0f
    with:
      build-workflow-ref: 86e3cbfc7b3a8093801f36bbfb2d7245bb50cb0f
