name: PR Validation

on:
  pull_request:
    branches:
      - release
      - side/ci-v2-release
    types: [opened, synchronize, reopened]
    paths:
      - ".ci/config.json"
      - ".ci/docker-matrix.json"
      - ".ci/README.md"
      - "linux-*.Dockerfile"
      - ".github/workflows/pr-validation.yml"
      - ".github/workflows/release.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pr-validation:
    # Keep tooling-ref equal to the SHA pinned in uses:.
    uses: runlix/build-workflow/.github/workflows/pr-validation-v2.yml@83f522fecbdd77b7875e4e474ddbdc78d978ac3e
    with:
      tooling-ref: 83f522fecbdd77b7875e4e474ddbdc78d978ac3e
      config-path: .ci/config.json
      legacy-matrix-path: .ci/docker-matrix.json
      validate-legacy-matrix: true
