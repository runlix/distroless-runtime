name: PR Validation

on:
  pull_request:
    branches:
      - release
    types: [opened, synchronize, reopened]
    paths:
      - ".ci/config.json"
      - ".ci/README.md"
      - "linux-*.Dockerfile"
      - ".github/workflows/pr-validation.yml"
      - ".github/workflows/release.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pr-validation:
    uses: runlix/build-workflow/.github/workflows/pr-validation-v2.yml@ci-v2-self-contained-preview-20260317
    with:
      config-path: .ci/config.json
