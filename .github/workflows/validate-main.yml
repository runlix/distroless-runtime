name: Validate Main

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - "release.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-main:
    name: validate-main
    uses: runlix/build-workflow/.github/workflows/validate-release-json.yml@89677e86055c532fa27ab5f660c939e3f5ebd6d8
    with:
      tool-image: ghcr.io/runlix/build-workflow-tools:sha-7f1f00fad88250b602361378a130370a5c35420e
      release-json-path: release.json
