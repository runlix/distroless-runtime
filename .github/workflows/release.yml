name: Release

on:
  push:
    branches:
      - release
      - side/ci-v2-release
    paths:
      - ".ci/config.json"
      - ".ci/docker-matrix.json"
      - ".ci/README.md"
      - "linux-*.Dockerfile"
      - ".github/workflows/release.yml"

permissions:
  contents: read
  packages: write

jobs:
  release:
    # Keep tooling-ref equal to the SHA pinned in uses:.
    uses: runlix/build-workflow/.github/workflows/release-v2.yml@a21b790251bd366c83b559f33626b32acfe1343b
    with:
      tooling-ref: a21b790251bd366c83b559f33626b32acfe1343b
      config-path: .ci/config.json
      legacy-matrix-path: .ci/docker-matrix.json
      validate-legacy-matrix: true
      release-artifact-name: release-metadata
